{
  "name": "bastra__move_document",
  "description": "[Bastra-recall (persistent memory)] Move a document (original + sidecar) into a different folder. The id stays stable; only paths and folder_path-frontmatter change.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Document id (slug)."
      },
      "folder_path": {
        "type": "string",
        "description": "Target folder relative to documents-root."
      }
    },
    "required": [
      "id",
      "folder_path"
    ]
  }
}