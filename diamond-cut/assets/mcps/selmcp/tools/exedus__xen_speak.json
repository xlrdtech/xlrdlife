{
  "name": "exedus__xen_speak",
  "description": "[Exedus (Xen core tools - voice/SMS/Beeper/vision/pane/escalate, 11 tools)] CANONICAL VVS reply — fires voice + dual-SMS (8672+9009) + dual-display vision via xen-reply-vvs wrapper. Use this for every reply to qi. Never use xen_shell to call bare `say`.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "Short gist (one sentence). Barge-in handles overlap."
      }
    },
    "required": [
      "text"
    ]
  }
}