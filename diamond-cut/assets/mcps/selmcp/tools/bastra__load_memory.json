{
  "name": "bastra__load_memory",
  "description": "[Bastra-recall (persistent memory)] Load the full content (frontmatter + body) of a single memory by id. Use this after recall() returns a hint with a high score, to read the full lesson before acting.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Memory id (the slug, no .md extension)."
      }
    },
    "required": [
      "id"
    ]
  }
}