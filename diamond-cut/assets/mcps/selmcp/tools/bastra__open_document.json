{
  "name": "bastra__open_document",
  "description": "[Bastra-recall (persistent memory)] Open a document in the system's default handler (macOS only). Use only when the user explicitly says 'open', 'show me' or similar.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Document id (slug, no .md extension)."
      }
    },
    "required": [
      "id"
    ]
  }
}