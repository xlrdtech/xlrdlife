{
  "name": "nats__jetstream",
  "description": "[NATS (center mesh / browser AIs / webchat / real-time knowledge multiplexer)] List JetStream streams (name + subjects + message count) on the live NATS server.",
  "inputSchema": {
    "type": "object",
    "properties": {}
  }
}