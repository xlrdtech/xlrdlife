{
  "name": "nats__request",
  "description": "[NATS (center mesh / browser AIs / webchat / real-time knowledge multiplexer)] NATS request/reply: publish to subject and await one response (timeout ms, default 8000). e.g. subject=\"external.gemini.query\" payload={q:\"...\"}.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "subject": {
        "type": "string"
      },
      "payload": {},
      "timeoutMs": {
        "type": "number"
      }
    },
    "required": [
      "subject"
    ]
  }
}