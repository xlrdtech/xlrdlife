{
  "name": "sqlite__query",
  "description": "[SQLite (SovereignBridge :6660 - session/xenmind/contacts/conversations/sources)] Run a SQL query against a store; returns JSON rows. e.g. store=sources sql=\"SELECT * FROM bookmarks LIMIT 50\".",
  "inputSchema": {
    "type": "object",
    "properties": {
      "store": {
        "type": "string",
        "enum": [
          "session",
          "xenmind",
          "contacts",
          "conversations",
          "sources"
        ]
      },
      "sql": {
        "type": "string"
      }
    },
    "required": [
      "store",
      "sql"
    ]
  }
}