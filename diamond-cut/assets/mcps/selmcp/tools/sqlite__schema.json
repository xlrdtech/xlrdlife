{
  "name": "sqlite__schema",
  "description": "[SQLite (SovereignBridge :6660 - session/xenmind/contacts/conversations/sources)] List tables + CREATE SQL for a store.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "store": {
        "type": "string",
        "enum": [
          "session",
          "xenmind",
          "contacts",
          "conversations",
          "sources"
        ]
      }
    },
    "required": [
      "store"
    ]
  }
}