{
  "name": "bastra__recategorize_document",
  "description": "[Bastra-recall (persistent memory)] Update folder, title, tags or category of an existing document. Refuses to overwrite externally edited sidecars unless force=true.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Document id (slug)."
      },
      "folder_path": {
        "type": "string",
        "description": "New folder relative to documents-root."
      },
      "title": {
        "type": "string"
      },
      "tags": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "category": {
        "type": "string",
        "enum": [
          "vertrag",
          "rechnung",
          "notiz",
          "code",
          "bild",
          "sonstiges"
        ]
      },
      "force": {
        "type": "boolean",
        "description": "If true, override the conflict-detection veto (use when the user explicitly accepts losing the external edit)."
      }
    },
    "required": [
      "id"
    ]
  }
}