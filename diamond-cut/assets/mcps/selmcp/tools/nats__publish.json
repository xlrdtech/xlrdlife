{
  "name": "nats__publish",
  "description": "[NATS (center mesh / browser AIs / webchat / real-time knowledge multiplexer)] Publish a message to a NATS subject (fire-and-forget). e.g. subject=\"omni.event\" payload={...}.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "subject": {
        "type": "string"
      },
      "payload": {}
    },
    "required": [
      "subject"
    ]
  }
}