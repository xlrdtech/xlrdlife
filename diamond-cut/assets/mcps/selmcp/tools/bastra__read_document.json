{
  "name": "bastra__read_document",
  "description": "[Bastra-recall (persistent memory)] Load a document's metadata + extracted text by id (returned from find_document). Use this before quoting or summarizing.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Document id (slug, no .md extension)."
      }
    },
    "required": [
      "id"
    ]
  }
}