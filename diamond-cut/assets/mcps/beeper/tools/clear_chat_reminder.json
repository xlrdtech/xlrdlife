{
  "name": "clear_chat_reminder",
  "description": "Clear a chat reminder.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "chatID": {
        "type": "string",
        "description": "Unique identifier of the chat."
      }
    },
    "required": [
      "chatID"
    ]
  }
}