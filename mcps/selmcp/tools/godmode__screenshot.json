{
  "name": "godmode__screenshot",
  "description": "[Godmode (Mac shell — run_command + full desktop automation)] Take a screenshot of the entire screen or a specific window",
  "inputSchema": {
    "type": "object",
    "properties": {
      "window_title": {
        "type": "string",
        "description": "Optional: capture only a window matching this title"
      },
      "save_path": {
        "type": "string",
        "description": "Optional: save to this path (default: returns base64)"
      }
    },
    "required": []
  }
}