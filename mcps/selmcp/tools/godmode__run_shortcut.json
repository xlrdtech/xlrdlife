{
  "name": "godmode__run_shortcut",
  "description": "[Godmode (Mac shell — run_command + full desktop automation)] Run a macOS Shortcut by name",
  "inputSchema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Shortcut name"
      },
      "input": {
        "type": "string",
        "description": "Optional: input text to pass"
      }
    },
    "required": [
      "name"
    ]
  }
}