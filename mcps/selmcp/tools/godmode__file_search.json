{
  "name": "godmode__file_search",
  "description": "[Godmode (Mac shell — run_command + full desktop automation)] Search for files using Spotlight (mdfind)",
  "inputSchema": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Spotlight search query"
      },
      "path": {
        "type": "string",
        "description": "Optional: limit search to this directory"
      },
      "max_results": {
        "type": "number",
        "description": "Max results"
      }
    },
    "required": [
      "query"
    ]
  }
}