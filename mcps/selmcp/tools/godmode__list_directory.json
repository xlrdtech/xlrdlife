{
  "name": "godmode__list_directory",
  "description": "[Godmode (Mac shell — run_command + full desktop automation)] List files and folders in a directory",
  "inputSchema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string"
      },
      "recursive": {
        "type": "boolean",
        "description": "List one level deep into subdirs"
      }
    },
    "required": [
      "path"
    ]
  }
}