# LAST KNOWN STATE
updated: 2026-06-30 (Matrix bridge live, DCZ invoice sent, RV rentals + hands-free tablet shipped)

## ‼️ SESSION 2026-06-29 EVENING → 2026-06-30 ‼️

### SHIPPED
- ✅ **DCZ invoice message** sent to Andre (+14702341117, Beeper chatID 396) AND LaTina (+17702565492, chatID 467) via Beeper MCP — both confirmed delivered (msg IDs 35499/35500). Message: balance invoice for DCZ social+website work, link hitthe.link/diamond-cut/invoice/balance/, domain connect + SEO + Voice Receptionist next.
- ✅ **hitthe.link/rv-rentals** (`E:\hitthe.link\rv-rentals\index.html`) — RV rentals Atlanta GA, 8 options, dark theme, mobile-first, pricing (daily/weekly), autoship live.
- ✅ **hitthe.link/hands-free-tablet** (`E:\hitthe.link\hands-free-tablet\index.html`) — hands-free tablet solutions, mounts/software/accessories, autoship live.
- ✅ **Ponytail Claude Code plugin** installed: `E:\.claude\plugins\cache\ponytail\85cc1d993527` (v4.8.4, enabled). 6 commands: ponytail, ponytail-review, ponytail-audit, ponytail-debt, ponytail-gain, ponytail-help. Installed via manual registration due to Windows EACCES .git cleanup bug.
- ✅ **Matrix bridge fully operational** — local Synapse at localhost:8008 (v1.145.0), access token sourced from M4 Beeper2 SQLite (`/Users/qi/Library/Application Support/Beeper2/account.db`). Room IDs discovered for key contacts:
  - Andre (DCZ GV) → `!QPAcJmfuQnjFqLsifu:mymatrix.local`
  - LaTina (DCZ GV) → `!VVpCwMOJEhYevxTSLw:mymatrix.local`
  - Luckie Goggins (WA) → `!hxiUnJSXOTMQYtvsgx:mymatrix.local`
- ✅ **`E:\bridges\matrix_send.py`** — Matrix send utility: `python matrix_send.py 396 "message"` routes via local Synapse GV bridge. Verified working (test event sent to Andre's GV room).
- ✅ **`E:\bridges\matrix_room_ids.json`** — contact→Matrix room ID map (all key contacts).
- ✅ **`E:\bridges\matrix_qi_token.txt`** — qi impersonation token for local Synapse (@xenadmin registered via shared secret, impersonates @qi:mymatrix.local).
- ✅ **`E:\bridges\matrix_chat_map.json`** — Beeper chatID → Matrix room ID map.

### IN-FLIGHT / BLOCKED
- ⚠️ **Beeper subscription EXPIRED** — qi not renewing. All outbound messaging now routes through `matrix_send.py` (Matrix local Synapse). Beeper MCP tool still works for reads but sends may fail.
- ✅ **Gmail↔Matrix two-way bridge LIVE (2026-06-30)** — `E:\bridges\email_twoway.py`. Passwordless OAuth (Thunderbird public PKCE client, scope `https://mail.google.com/`) minted on M4 via `gmail_oauth_mint.py` (now has `url`/`exchange`/`listen` modes; consent done in M4 Safari, captured by the `listen` HTTP server on 127.0.0.1:47533). Token at **`/Users/qi/.xen/gmail_oauth.json`** (M4) → copied to **`E:\bridges\gmail_oauth.json`** (Nitro) via `rclone copyto m4:.xen/gmail_oauth.json`. **VERIFIED A→Z**: inbound 16+ emails posted to Matrix room `!BjcrkBmUNxAIIpTxPg:mymatrix.local` w/ Gmail thread-map (`email_thread_map.json`); refresh works (`gmail_token_cache.json` written); SMTP XOAUTH2 send OK (self-test delivered + re-bridged); threaded Matrix reply (as @qi, m.in_reply_to) → `outbound: 1 replies emailed` → arrived back in Gmail thread. Scheduled task **XenEmailTwoway** runs `python -X utf8 email_twoway.py once` every 2 min (State=Ready). Mailbox = **selfexec.com@gmail.com**. Bug fixed: IMAP `authenticate` callback must return RAW SASL string (imaplib base64-encodes it); SMTP path pre-encodes.
- ⚠️ **Test message to Andre** — `matrix_send.py` sent a "test from xen matrix bridge" to Andre's GV room during verification. May show on his phone.
- ⚠️ **Matrix conductor wiring** — `matrix_send.py` not yet wired into xen-conductor inject pipeline. Beeper MCP is still the fallback until this is done.

### NEXT BUILD ORDER
1. Wire `matrix_send.py` into conductor/inject pipeline so voice-triggered send commands route through Matrix instead of Beeper MCP
2. ✅ DONE — Gmail two-way bridge live (XenEmailTwoway task).
2b. ✅ DONE — **Claude Code Conductor LIVE (Conductor OSS, 2026-06-30)**. Repo qi sent (`conductor-oss/conductor`) cloned to `E:\stack\conductor`, deployed via `docker/docker-compose.yaml` (conductor:server 1.2GB + Redis + ES7) → UI/API **http://localhost:8000** (Mac: http://100.80.76.79:8000 over Tailscale), gRPC :8127. `claude_worker.py` polls `claude_code_task` → runs `claude -p` (sonnet) → returns; `claude_conductor_setup.py register|start|status` drives the 2-step `claude_code_demo` (generate→review). VERIFIED end-to-end: workflow COMPLETED, output piped between Claude tasks. See [[claude-code-conductor]].
   OPEN: (a) durable Google-cookie "fountain" for mautrix-gvoice (GV keeps 401'ing — static cookie snapshot from Nitro datacenter IP gets revoked; needs live logged-in browser rotating __Secure-1PSIDTS). (b) wire conductor worker to round-robin the claude.ai 3-session pool (per-account CLI creds, E:\bridges\claudeai_sessions.json) for true parallel no-collision lanes.
3. Map remaining contacts (Luckie WhatsApp #33, Telegram #68, Hittl.ink WA group #416) to Matrix rooms via matrix_send.py
4. Reboot-safety: ensure matrix_send.py deps + Synapse running on startup

---

updated: 2026-06-29 (XEN RSS notification center + omni→XOS wiring LIVE)

## ‼️ XEN RSS SHIPPED — notification center + feed + omni→XOS wiring (2026-06-29) ‼️
qi: "i need xen rss so create it. rss can be the hitthe.link notification center and also feed into the omni otlp and omni otlp should feed the xos notification center." DONE end-to-end:
- ✅ **hitthe.link/rss/** = live notification center (`rss/index.html`) — client-reads `/xen/live.json` `by_capability[].recent[]`, flattens→chronological feed, category filter chips, auto-refresh 30s. Verified rendering real mobile Chrome (32 events/16 filters, shot rss-center.png).
- ✅ **hitthe.link/rss/feed.xml** = real RSS 2.0 (subscribable) + `rss/feed.json`. Generator `rss/build_rss.py` (reads live.json → writes feed.xml+feed.json → injects NEW events to omni).
- ✅ **RSS → omni OTLP**: "omni" = **omnimind** (`/Volumes/M4/sync_/exedus/dev_/omnimind/omnimind.js`, :4441). build_rss POSTs new events to **`http://127.0.0.1:4441/api/omni/event`** (a NO-AUTH bypass path in omnimind's AUTH_BYPASS_PATHS; payload uses redundant src/source + body/text/preview so any consumer maps). VERIFIED POST=200.
- ✅ **omni → XOS notif center**: omnimind broadcasts the event to **`xen.xlrd.org/events`** SSE (via `__mirrorBus.emit('event',...)`), which **XOS already consumes** (`xos/app.jsx`: `SSE_URL=xen.xlrd.org/events` → EventSource.onmessage → setNotif → NotificationToast/DockNotifTicker). VERIFIED: test event appeared on the live SSE stream.
- ✅ **Regen daemon**: launchd `com.xen.rss-builder` (StartInterval 600s, RunAtLoad) → `/opt/homebrew/bin/python3.11 rss/build_rss.py`, log `/Users/qi/logs/xen-rss-builder.log`. feed.xml/json deploy on receipts-builder's next commit cycle; /rss/ page is live-by-default. Deploy = pause receipts-daemon + reset --hard origin/main + cherry-pick + push.
- ✅ **EXTERNAL RSS INGESTION** (qi: "can I easily add rss feeds to it?" → yes): `rss/sources.json` = `{feeds:[{name,url,category}]}`. build_rss.py fetches each server-side (urllib+UA, ElementTree parse RSS2.0 `<item>` + Atom `<entry>`, no CORS), writes `rss/external.json`, merges into feed.json/feed.xml, injects NEW items to omni→XOS. /rss/ page now fetches live.json + external.json and merges (each feed = its own filter chip). VERIFIED LIVE: Hacker News (hnrss.org/frontpage) → 20 items merged, 52 total (shot rss-center.png). **To add a feed: append a URL to rss/sources.json** (or paste one to Xen). Anthropic rss.xml 404'd — removed.

## ‼️ Matrix bridges + hitthe.link Connect page + bridge consolidation (2026-06-28→29)

## ‼️ MATRIX BRIDGES + hitthe.link/connect + GOOGLE VOICE LIVE (2026-06-28) ‼️
qi's goal: BPGS1 = matrix bridges (gv/slack/telegram/whatsapp/ig/fb/discord/linkedin); BPGS2 = overhaul hitthe.link home + test like a human. CONSTRAINTS (verbatim): "beeper api is the only twilio allowed" (Twilio ONLY via Beside API), "docker only on nitro!" (containers NEVER on M4).
- **BPGS2 DONE + VERIFIED 10/10:** hitthe.link home (`E:\_ios\`, repo xlrdtech/hitthe.link, GH Pages). Rewrote `js/sites.js` = Spotlight search (filters all 167 sites live) + iOS App Library (all sites in category folders, searchable) + dock. Drove the LIVE site in real Chrome @ iPhone viewport, 10/10 checks, 0 console errors (test `E:\htltest\test.mjs`, shots `E:\htltest\shots\1-6*.png`). sw.js bumped htl-v4.
- **Infra UP:** Synapse homeserver LIVE on M4 (`/Users/qi/synapse`, env311/py3.11, sqlite, server_name mymatrix.local, fronted matrix.xlrd.org via godmode-mac CF tunnel). **7 mautrix bridgev2 containers RUNNING on Nitro Docker** (WSL Ubuntu, DOCKER_CONFIG=/tmp/dc): whatsapp/telegram/discord/gvoice/meta(FB+IG)/slack/linkedin, configs `/home/lord/bridges/<net>/config.yaml`, all grant `@qi:mymatrix.local`=admin. **Homeserver holds qi's REAL synced history** (hundreds of @whatsapp_*/@telegram_*/@meta_*/@discord_* ghosts) — bridges WERE logged in before; backfill survived.
- **@qi password RESET to `0000`** (was failing both pws): `/Users/qi/synapse/reset_qi.py` (hash_password + sqlite UPDATE). matrix.xlrd.org is behind Cloudflare which **403s the default python-urllib UA** → always send a browser User-Agent. CS API CORS = `*` (browser can talk direct, incl authed media).
- ‼️ **CANONICAL ACCOUNT = @self (NOT @qi)** — qi's client logs in as @self; @self already held the original Telegram session (xlrdtech) + 32 rooms. I'd wrongly set up bridges under @qi; qi said "add them all to self". CONSOLIDATED: logged @qi OUT of all 4 (qi_logout.py), re-logged @self IN. Bridges block same remote-account under two matrix users (meta: "@qi already logged in"; linkedin li_at got invalidated) so logout-first is required. Default account for ALL scripts now @self (bridge_login.py default, accept_invites BRIDGE_USER, inbox_watch, connect page login user:'self', @self m.direct set for 6 bots via self_mgmt_rooms.json).
- ✅ **CONNECTED on @self 3/8: Telegram** (xlrdtech 727680186), **Facebook** (East Allen 100013404306410), **Instagram** (XLRD999 113155480083272) — verified in bridge logs (IG actively backfilling messages). ~116 rooms, chats populating. **Telegram RECOVERED autonomously**: was UNKNOWN_ERROR (`MSGID_DECREASE_RETRY` — transient MTProto msg-id/clock-sync, NOT revoked auth) → fixed by `docker restart bridges-mautrix-telegram-1` (no qi re-scan). Same restart trick cleared a stuck meta command-state.
- ✅ **SLACK CONNECTED — created FULLY AUTONOMOUSLY** (2026-06-29, qi asked 6×): from qi's app.slack.com cookie export alone, drove the dev portal HEADLESS (killed OneTrust overlay) → config token → apps.manifest.create (app A0BETCVKM6C) → OAuth Allow → xoxb → xapp app-level token → `login app` → `T09D3V509ED-U0BE2QDDJCC (SELFEXEC - xen) CONNECTED`. Reusable recipe: memory [[autonomous-slack-app-from-cookie]]. Also shipped: robots.txt+sitemap.xml (173 urls); 2 Hermes free-LLM evals (Nemotron build.nvidia.com `nemotron-3-super-120b-a12b` free/OpenAI-compat ~40rpm; OpenRouter `:free` rotation, $10-once→1000/day, primary qwen3-coder:free); Comet profile→`--profile-directory` map (14 profiles, Default=selfexec … "Profile 10"=lord@xlrd.org).
- ⚠️ **GV + LinkedIn keep re-expiring to BAD_CREDENTIALS** — Google/LinkedIn rotate session cookies aggressively from a non-origin IP; fresh exports work briefly then drop. Needs a durable refresh-token flow or periodic re-export. WhatsApp+Discord still QR-pending.
- ✅ **GV RECONNECTED on @self** (4/8 now: Telegram, FB, IG, GV) — qi pasted fresh voice.google.com cookies (gv_fresh.json), CONNECTED. (Had regressed to 401 when @qi logout revoked the shared Google session — LESSON: logging out one matrix user's cookie-shared login revokes the other's.) 84 invites accepted via accept_invites.py.
- ⚠️ **LinkedIn STILL BAD_CREDENTIALS even with fresh cookies** — error `li_at cookie was deleted` on realtime/connect. LinkedIn invalidates li_at if the SOURCE BROWSER stays open (rotates token). FIX (per bridge's own instruction): qi must export from an **Incognito tab and CLOSE it before** injecting. li_cookies.json has the latest attempt. WhatsApp+Discord=QR, Slack=cURL still pending.
- ✅ **SHIPPED /life omni-inbox section** (qi: "autoship the overview and action items etc and update /lifec"; /lifec = /life). Added `id="omni"` section to `life/index.html` (repo) = "Matrix bridges — one inbox": 8 network status cards (4 connected green / 4 action amber) + action-items list w/ exact steps + Open Connect CTA. LIVE hitthe.link/life, verified rendering in real Chrome (E:\htltest\life_shot.mjs, shot life-omni.png). Deploy fought daemon-receipts rebase conflicts → resolved via pause-daemon + `git reset --hard origin/main` + `git cherry-pick <commit>` + push (commit on origin b523a2bf).
- ⚠️ **@self NEEDS qi action:** **LinkedIn** — old li_at expired during the move, qi must re-export linkedin.com cookies FRESH → inject `BRIDGE_USER=self python bridge_login.py linkedin <file>` (linkedin needs 3 full-id fields: cookie_header + synth x_li_track/x_li_page_instance, see [[mautrix-login-management-room]]). **Telegram** — @self session xlrdtech in UNKNOWN_ERROR, needs QR re-scan. **WhatsApp+Discord** — QR scan at hitthe.link/connect (now @self). **Slack** — cURL (xoxc). Cookie files dropped in M:\sync_\inbox_\ auto-inject to @self (watcher). qi pastes Cookie_Editor exports (array of {name,value,domain}) → I convert to JSON object `{name:value}` → send in the bot's MANAGEMENT room. GV: cookies file in inbox. Meta: `login facebook` then cookies (mode baked into login cmd; meta needs `login <facebook|messenger|instagram>` NOT bare login). Generic injector = **`E:\bridges\bridge_login.py <net> <cookiefile> [mode]`** (deterministic: cancel→login [mode]→send JSON→poll). Also gv_login.py/gv_finish.py/build_gv_payload.py.
- ✅ **ALL 3 QR NETWORKS VERIFIED RENDERING** (whatsapp/telegram/discord) via `login qr` in their mgmt rooms → real scannable QR PNGs (`E:\bridges\wa_qr.py`, `disc_qr.py`). qi just needs to scan at hitthe.link/connect. WhatsApp mgmt !oaMFhtkCTlMtgACxGW, telegram !VjrTIQVYUhdrJtwpOs, discord !bCWAeLiSvihBVoxSLt (created this session).
- 🔑 **KEY LEARNING (memory [[mautrix-login-management-room]]):** mautrix bridgev2 only consumes interactive login input (cookies/QR) in the bot's MANAGEMENT room (first DM, greeted "marked as your management room"). A fresh DM accepts prefixed commands but SILENTLY DROPS step input. Set `@qi` `m.direct` = each bot→its mgmt room (`E:\bridges\set_mdirect.py`, saved `mgmt_rooms.json`): gvoice !ihHntSuCgiRWjAzYEV, whatsapp !oaMFhtkCTlMtgACxGW, telegram !VjrTIQVYUhdrJtwpOs, meta !DpFlGyLUjfBGzInhfv, slack !vrNXhZOAtUkChtEjwG, linkedin !ZSGFELxqMmpzKsInFU. discord = no mgmt room yet (creates on first tap).
- ✅ **hitthe.link/connect LIVE + VERIFIED 3/3** (`E:\_ios\connect\index.html`): login gate (@qi/0000) → 7 network cards → tap a network = resolves its mgmt room (via m.direct), auto-fires login once bot joins, renders live QR inline (wa/telegram/discord) or cookie/cURL paste box (gvoice/meta/slack/linkedin). WhatsApp live QR rendered+scannable in real Chrome (test `E:\htltest\connect_test.mjs`, shot `c3-whatsapp-qr.png`). Bridge-bot console design = handles every flow.
- **BLOCKED on qi (irreducible human auth):** remaining 6 networks need qi's action — WhatsApp/Telegram/Discord: open hitthe.link/connect→tap→scan live QR; Meta(FB+IG)/Slack/LinkedIn: export cookies like GV→I inject (or paste in connect page). WhatsApp has an ongoing login session waiting for a scan.
- **NEXT:** (1) qi scans WhatsApp QR / provides remaining cookies; (2) verify each connects via list-logins + that history syncs; (3) optional: add a "Connect" tile to sites.json so it's on the home; (4) email arbitrage/triage via Claude Tag (Gmail connected) — not started.
- Deploy ops note: receipts-builder daemon (M4 launchd com.xen.receipts-builder) auto-commits to this repo and RACES pushes → pause it (`launchctl bootout gui/$(id -u qi)/com.xen.receipts-builder`) for clean commits, re-enable after (`launchctl bootstrap`). Deploy = scp Nitro `E:\_ios\connect\index.html` → M4 repo → commit (no em-dash/attribution) → `git -c rebase.autoStash=true pull --rebase` (or `rebase -X theirs` if daemon commits conflict) → push → poll live with `?cb=` cache-buster.


## ‼️ XOS CENTER-PANEL BROWSER-IN-BROWSER SPEC SHIPPED TO CANON (2026-06-27) ‼️
qi asked to evaluate two Perplexity research docs (browserOS controllability + opensource browserbase) then build the browser-in-browser architecture. Ran a 29-agent ultracode workflow (ground→3 designs→12 judges→synthesize→7 adversarial verifiers→spec). Output: **`E:\.xen\canon\XOS-center-panel-browser-in-browser-spec.md`** (276 lines, codename **LMHJ** = Live Mirror with Isolated Host Jails).
- CORE RULE: control surface is ALWAYS a host-side per-app isolated Chromium context on Nitro (`browser.newContext()` + profile per app); render surface is one of 3 modes (mirror canvas / iframe / dom) the host negotiates per tab; agent NEVER drives PWA DOM, only the host. Joined by ONE new Nitro daemon **XBB `:8044`** (clone dcz-edit-server skeleton) over a Zod envelope `{v,ch,tabId,epoch,seq,ts,type,payload}`. ch:input (human finger) cryptographically separate from ch:act (agent, conductor/NATS-only). MODE=mirror (CDP screencast) is the safe default; iframe opportunistic; dom is per-app proven opt-in. Tier-3 window.open breakout DELETED (broken on standalone iOS).
- LIVE-PROBE FINDINGS (workflow re-probed, corrected stale design assumptions):
  - CDP `:9222` discovery is **EMPTY** today → must relaunch visible Chrome `--remote-debugging-port=9222 --remote-allow-origins=*` before XBB can attach (pre-flight gate #1).
  - auth-broker `:4477` is **DOWN** → spec scopes it to phase-2 (2FA); MVP uses warm cookies + login-wall detector.
  - Funnel root **collision**: dcz-edit `:8788` occupies `/` → need a fronting reverse-proxy path-routing `/xos`→`:8044` (Open Decision #2).
  - Seat token **GREEN** (re-probed `auth-bridge-status.json`: expired:false, minutesLeft 426, sk-ant-oat01 Max seat) — earlier "-276 expired" was a stale snapshot; spec mandates live-read at arm time.
  - New deps NOT present: `ws`, `playwright-core`, `chrome-remote-interface` (the "exists at E:\hermes" note was false) → `npm i` under E:\xen or raw-WS-to-CDP. All OSS, $0 marginal/task holds.
  - Conductor scope regex `{1,40}` overflows on `xos.tab.<uuid>` → raise to `{1,64}` in xen-conductor.js OR use short 6-hex tabIds + hard pre-inject match assert (else silently routes to HOME).
- 6 OPEN DECISIONS FOR QI (spec §11): (1) MVP first app (assumed Gmail), (2) Funnel-root fix path-route vs repoint, (3) conductor scope cap raise vs short tabIds, (4) 2FA/:4477 timing phase-2 vs now, (5) which apps get MODE=dom fidelity PoC, (6) stealth OFF for first-party logged-in apps + Camoufox only for Cloudflare/DataDome.
- qi DECISIONS (this session): MVP app = **vvsvei** (not Gmail; first-party/cooperative, no auth wall, no ban risk). Build = GREEN LIT (incl conductor edit allowed). Funnel-root = "huh?" → deferred; bind XBB MESH-ONLY for now, no dcz/Funnel disruption.

## ‼️ PROTOCOL CORRECTION — qi's research IS the spec; build is now ALIGNED (2026-06-27) ‼️
qi pointed to `M:\sync_\brain_\agents\xen\xen_memories\062626.md` then to his two Downloads docs (`is browserOS the absolute most controllable for 24.md`, `opensource browserbase.md`) as "how I want it done" = the **browser-protocol research** (the "browser in browser in browser" / browser-of-browsers). HOLD-don't-guess rule satisfied: the docs ARE the doc. See memory [[canon-xos-browser-of-browsers]].
- qi's CANONICAL protocol (Doc 1, "browser in browser in browser"): middle panel = `tab workspace manager` + `pluggable page loader`, 3 modes IN THIS ORDER: **Tier 1 iframe (DEFAULT, nested managed document surface)** → **Tier 2 proxied/re-host through own origin** (framing-blocked; mirror is interim fallback) → **Tier 3 fail-open breakout** ("unless it's a faith"). Kernel objects: tabRegistry, runtimeModeResolver, iframeHost, proxyHost, breakoutPolicy, glassOverlayManager, drawerManager, agentActionBus. Agent targets the middle browser's tab/runtime graph (ID + state + DOM-snapshot + action bus per tab).
- MY EARLIER LMHJ ERROR (corrected): I made host-side **screencast the DEFAULT** and DELETED breakout. Wrong. Screencast is qi's Tier-2 *mirror* fallback, NOT the default; breakout is Tier 3 (kept). For vvsvei (hitthe.link, same-host) the protocol = **Tier-1 iframe**: native, no host Chrome, no screencast, no bridge. I over-built a remote-browser rig for an app that should just be iframed.
- ALIGNED + SHIPPED (commit e1c7d6f6): `E:\hitthe.link\xos\xos-browser.jsx` rewritten as the kernel — `runtimeModeResolver` (iframe default; auto-degrade to mirror if framing blocked; breakout via ↗), `IframeHost` (Tier 1), `MirrorHost` (Tier-2 interim = the XBB screencast). VERIFIED in real Chrome: vvsvei renders as `mode="nested"`, real `<iframe src=hitthe.link/vvsvei>`, hasCanvas=false (screenshot `E:\xen\xos\verify-iframe.jpg`). Canon spec §3 corrected to qi's tier order.
- XBB (:8044) + host Chrome (:9223) + tailscale serve (:8444) STAY UP as the Tier-2 mirror fallback for framing-hostile third-party apps. The REAL Tier-2 build = `proxyHost` (re-host through own origin so a same-origin iframe works + agent gets DOM) — NOT YET BUILT, next.

## ‼️ XOS MVP BUILT + VERIFIED END-TO-END (2026-06-27, codename LMHJ) ‼️
The browser-in-browser MVP is WORKING and proven with on-disk + screenshot evidence. NOT yet reachable from qi's phone (one transport gap, below). All under new home **`E:\xen\xos\`** (self-contained, own node_modules; deps ws + chrome-remote-interface + playwright-core installed).
- PRE-FLIGHT findings: **:9222 was squatted by comet.exe** (Perplexity Comet, no CDP /json) → did NOT use it. :9200 = browseros_server ghost. Solution: dedicated **headless Google Chrome on CDP :9223**, isolated profile `E:\xen\xos\profiles\vvsvei-01` (BrowserOS chrome.exe is a launcher stub that forwards+exits; do not use it for the jail). Seat token GREEN.
- **`E:\xen\xos\xen-xos-bridge.js` (XBB, :8044)** built + running. Verified: POST /tab opens a CDP page target + mobile emul (440x956@2x); per-tab WS `ws://HOST:8044/tab/<id>?t=<token>` streams BINARY frames (8-byte header = uint32 epoch + uint32 seq, then JPEG); human input `ch:input` taps relayed via CDP Input.dispatchMouseEvent with EPOCH GATE (stale-epoch tap REJECTED, verified); agent path `POST /xos-act` (goto/read/click/screenshot) token-gated, separate from human WS = the ch:input/ch:act split is real. Screencast is VIEWER-GATED (idle tab seq=0, streams only while a WS viewer attached, stops on last disconnect — verified, no idle CPU burn). Token = dedicated `E:\xen\xos\bridge-token.txt` (NOT the seat token). Audit → `E:\xen\xos\xos-audit.jsonl`.
- **PWA render surface** added ADDITIVELY to `E:\hitthe.link\xos\` (repo xlrdtech/hitthe.link): new `xos-browser.jsx` (self-mounting overlay, exposes `window.XOS.open(url)`/`xos:open` event, canvas mirror + tap relay + native-input overlay), one `<script>` line in index.html, scoped CSS appended to styles.css. ZERO edits to the 71KB app.jsx. Overlay sits above voice home, leaves bottom-glass clearance.
- **VERIFIED IN A REAL BROWSER** (verify-pwa.js via CDP, screenshot `E:\xen\xos\verify-pwa.jpg`): loaded /xos over local http, `window.XOS.open('https://hitthe.link/vvsvei')` → live vvsvei pixels (real feed, real timestamps) painted in the canvas inside the XOS shell, "● live mirror" chrome + close, bottom glass intact. Browser-in-browser-in-browser CONFIRMED.
- RUNNING NOW: host Chrome pid (headless :9223), XBB (:8044), local static serve (:8055 for testing only). Reproducible launcher: **`E:\xen\xos\start-xos.ps1`** (idempotent). NOT reboot-safe yet (no ONLOGON task — pending).
- ✅ TRANSPORT SOLVED + LIVE: **canonical live URL = `https://nitro.salmon-alkaline.ts.net:8444/xos/`** (tailnet-only, valid LE cert, open on any tailnet device incl qi's iPhone). `tailscale serve --bg --https=8444 http://127.0.0.1:8044` (NOT funnel; dcz :443→8788 + nitro11→3215 + SSH tcp:10000 all intact; :8443 was http.sys/System, avoided). XBB now also SERVES the PWA static at `/xos/` from `E:\hitthe.link\xos`, so page-origin == bridge-origin.
  - WHY same-origin: Chrome **Local Network Access** blocks a PUBLIC origin (hitthe.link) from fetching a LOCAL/tailnet IP (`LocalNetworkAccessPermissionDenied`). So hitthe.link/xos → tailnet bridge FAILS on Chrome desktop/Android (works on iOS WebKit, which ignores LNA, but unverified on-device). Serving the PWA from the mesh node makes it same-origin = no CORS, no LNA, works everywhere. VERIFIED via a Chrome with `--host-resolver-rules` (phone-path sim): live=true, vvsvei pixels painting, screenshot `E:\xen\xos\verify-live.jpg`.
  - Auth: PWA auto-fetches `/ticket` (tailnet-reachability = the gate) → no secret in the public repo. `xos.bridge` localStorage override for local dev (`http://localhost:8044`).
- SHIPPED to GH Pages (repo xlrdtech/hitthe.link, commits 1cfa5056 + 8adf3517): xos-browser.jsx + index.html + styles.css. hitthe.link/xos works for everything; the live-mirror feature there only works on iOS (LNA) — the ts.net URL is the cross-browser canonical surface.
- RUNNING: host Chrome headless :9223 (jail), XBB :8044 (serves API + PWA), tailscale serve :8444. NOT reboot-safe yet (no ONLOGON task). Verify chrome :9224 + local serve :8055 are TEST-ONLY (can be killed).
- NEXT: (1) reboot-safety — `XenXosBridge` ONLOGON task (start-xos.ps1) + watchdog + register `tailscale serve` persistence; (2) conductor `[scope:xos]` lane → POST /xos-act for VOICE-DRIVEN open + narrate via xen_speak (the agent control path; human tap already works); (3) on-device iPhone test of the ts.net URL; (4) generalize: host probe + 3-tier (iframe for cooperative), warm pool, multi-app contexts, phase-2 agentcookie/auth-broker for logged-in apps, phase-3 MODE=dom for heavy-touch.

## ‼️ HERMES-AS-FRONT CUTOVER (2026-06-25) — typing `xen` now fronts the HERMES AGENT ‼️
qi's true design (whole "Claude Code dispatch cli" thread): Hermes = the ONE brain he talks to, on his Claude. Built + (mostly) verified this session:
- **Hermes Agent (Nous v0.13.0) MOVED C:\Users\selfe\AppData\Local\hermes → `E:\hermes`** (E:-only). `HERMES_HOME=E:\hermes` (User). Launchers = `.cmd` wrappers in `…\venv\Scripts\` (pip .exe broke on move → renamed *.broken-cpath); editable finder repointed C:→E:; chalk TUI files restored. `hermes --version` → `Project: E:\hermes`. See memory [[hermes-agent-on-nitro-e-drive]].
- **config `E:\hermes\config.yaml`**: model `xen` / base_url `http://127.0.0.1:8021/v1` (the **WARM lane** — was :8020 fork) / api_key ${XEN_API_TOKEN} (in `E:\hermes\.env`).
- **Gateway `E:\xen\xen-openai-gateway.js` :8020 — fixed but SUPERSEDED for Hermes** (fork = cold-start per call, slow/flood-prone, wrong for an interactive front). Fixes still valid for other BYOM tools: XEN_CLAUDE_BIN=E:\.local\bin\claude.exe; `--bare`→`--dangerously-skip-permissions`; lean cwd=C:\Windows\Temp + CLAUDE_CONFIG_DIR=E:\.claude-gateway. Memory [[xen-gateway-nitro-fixes]].
- ‼️ **WARM BACKEND = THE FIX (WORKING, VERIFIED ~3s/turn):** `E:\xen\xen-warm-lane.js` (:8021) keeps ONE persistent `claude -p --input-format stream-json` lane warm (conductor pattern) → SessionStart once at spawn, every turn = pure inference. Reboot-safe: **XenWarmLane** task (ONLOGON, auto-restart) → `E:\xen\start-warm-lane.bat` → node (logs `E:\xen\xen-warm-lane.log`). Bugs fixed building it: (a) `-p`+`stream-json` **needs `--verbose`** (else exit code=1); (b) Hermes sends **`stream:true`** → server must return **SSE** not JSON (else empty replies); (c) lean config + cwd=Temp (no hooks → fast+silent). Memory [[hermes-warm-lane-backend]] (file write pending — C:\ memory dir was OneDrive-locked; reconstruct from here).
- ‼️ **CUTOVER LIVE:** `/Users/qi/bin/xen` (→symlink `xen-router`) `start)` case launches `E:\xen\bin\xen-hermes.ps1` (immortal `hermes chat`; SELF-HEALING — falls back to the Claude pane if the TUI can't hold the console). The Hermes TUI DID render over qi's real ssh ConPTY (prompt_toolkit works when attached; my headless tests couldn't replicate that). **VERIFIED end-to-end: `hermes -z` → real answer in 3s via warm lane (stream=true log confirmed).**
- **qi action: RELAUNCH `xen`** (Ctrl-C the pane, retype `xen`) so the Hermes pane loads the new :8021 warm config. Revert cutover: `perl -0777 -pi -e 's/xen-hermes\.ps1/xen-brain.ps1/g' /Users/qi/bin/xen`. Backup: `/Users/qi/bin/xen.bak-prehermes-cutover`.
- CLEANUP done: box CPU 100%→low (killed duplicate hermes sessions + orphan claudes; top CPU hogs were MsMpEng/WSL/Docker, not claude). OPEN: warm lane is single-session/serial (one convo at a time); temp repro files `E:\xen\tmp-*.js` deletable. Memory [[type-xen-front-is-hermes-agent]] [[hermes-agent-on-nitro-e-drive]].

## ‼️ RESUME VERIFY (2026-06-25) — STALE CHECKLIST CLEARED ‼️
A resume handed an old checklist claiming the inject-daemon cutover was still pending (pid 24200 running old code; conductor not reboot-safe). VERIFIED LIVE STATE contradicts it — all already done later that session:
- inject daemon: pid 24200 GONE. Live daemon = pid 49104, runs `E:\Exedus\windows-universal\xen-windows-universal\windows\inject-queue-daemon.ps1` (NOT E:\.xen\ — that path doesn't exist; canon refs were wrong). That live file HAS the `/route` patch (lines 102-116: POST :8030/route, 3s timeout, pane fail-safe). Single instance.
- conductor :8030: alive pid 55928 (`node E:\xen\xen-conductor.js`), health ok. fast lane alive + work-lane POOL pre-warmed (pool-1f670a, pool-ae6ce7 alive) = the "hide ~9s hook" open item is addressed.
- watchdog: pid 68184 (pwsh) running — conductor self-heal confirmed. Reboot-safe via Startup\start-conductor.bat.
- LESSON: canonical daemon path is E:\Exedus\...\windows\, NOT E:\.xen\. Always match Win32_Process.CommandLine to the file on disk before patching.
GENUINELY OPEN: scope registry #3 (no source data exists yet — do NOT fabricate), verbatim ES + alias routing #4, Hermes-is-front collapse (risky w/o qi: kills interactive TUIs).

## ‼️ MVP STATUS — CANON + VVS = SOLID (2026-06-25) ‼️
qi defined the MVP as exactly two things working together: **canon + VVS**. Both now verified end-to-end:
- CANON: `E:\.claude\CLAUDE.md` written + active — forces every session to READ canon (GLOBAL→LAST_KNOWN_STATE→agents/xen-main→scope) at cold-start and WRITE BACK to LAST_KNOWN_STATE after tasks. No re-explaining context. Mirrored to M4 (M:\sync_\brain_\canon\CLAUDE-nitro-bootloader.md).
- VVS: `xen_speak` → xen-say-worker (M4 pid 977, pane %16) verified "vvs fired" — canonical path, NOT Ava. Health green (4441/4445/4446/4447/4448/4442).
Everything else (verbatim ES index, full alias routing, Hermes-over-XEN, public ingress) is OPTIONAL until qi says otherwise.

## ‼️ XEN FULLY STARTED — VERIFIED 2026-06-25 (omni-sweep, 8 agents parallel) ‼️
`started: true` — all 7 subsystems GREEN + functionally proven (not port-open-only):
- Conductor :8030 (route handled, fast+pool lanes) · VVS voice (ports 4441/45/46/47/48, say-worker pid 977, xen_speak fired) · inject daemon + cutover (both pane + CONDUCTOR HANDLED in log) · canon+M4 mirror · gateway :8020 (/v1/models has xen+xen-warm, pid 19420) · Hermes narrator M4 (launchd com.xen.hermes-narrator pid 84208) · auth-bridge (token fresh, 60min).
- FIXED same sweep: 4 duplicate inject-queue-daemon instances were racing the same queue (double-injection risk). Collapsed to ONE (pid file now matches) + HARDENED inject-queue-daemon.ps1: startup kills sibling instances; loop steps down if a newer instance claims the pid file. Singleton now race-proof.

## ‼️ HERMES IS THE FRONT — collapse in progress (2026-06-25) ‼️
CORRECTION to earlier architecture: qi's model = **Hermes is the ONE brain he talks to** (not "talk to Claude, Hermes hidden"). Hermes = the conductor front; worker lanes (conductor :8030) = its hands; VVS = its voice; Claude worker-lanes = under it, never addressed directly. The brain in the canonical pane (xen:0 / %31, via xen-brain.ps1 over M4-ssh) now launches AS Hermes: `claude --continue --append-system-prompt <E:\.xen\hermes-identity.md>` (immortal loop). 
- TWO-BRAIN BUG (root of qi's confusion): a duplicate interactive Claude (pid 85796, this session) ran OUTSIDE the canonical pane while the cold %31 canonical claude also caught injects. Collapse = E:\xen\collapse-to-hermes.ps1 kills interactive TUIs (keeps headless conductor lanes + the immortal loop); loop relaunches ONE Hermes resuming the latest convo.
- If you (a fresh Hermes) are reading this after the collapse: you ARE Hermes, the sole brain. Read hermes-identity.md. The conductor (:8030) is your hands; VVS is your voice. Confirm you're the only interactive claude (no input-format-less duplicate besides you).
- Backups: xen-brain.ps1.bak-prehermes. Identity: E:\.xen\hermes-identity.md.

## RUNNING SERVICES (verified today)
- Model gateway :8020 (node, XenOpenAIGateway ONLOGON task — reboot-safe). Verified answering BrowserOS live.
- OTLP/omnimind :4441 up. Voice say-worker alive on M4 (pid was 977). Inject-queue-daemon present (E:\.xen\inject-daemon.pid).
- Auth-bridge: token fresh in E:\.xen\current_token.txt (team / max-5x). XenAuthBridge refresh task.
- CloudMounter filename sanitizer: launchd com.xen.filename-sanitizer on M4 (every 300s) — vault clean.

## SHIPPED LIVE TODAY (all verified)
- DCZ (hitthe.link/dcz): full polish — scroll animations every section, TikTok added, artist text un-clipped, video art full-bleed rectangles (no whitespace), portrait footer symmetric, site-wide line-wrap audit; IG link fixed to live @diamondcutzstudio (singular; plural was dead).
- desk (hitthe.link/desk): mobile-first portrait + full 360 orbit + hyper-real files (thickness, contact shadows, type tags). PWA + drop pipeline intact.
- L7S docs both brands: /l7s/docs (L7 Solutions) + /l7s/docs/hero (L7S Hero) — same 6 docs, fillable + in-browser e-sign (doc.js), terms/privacy got acknowledgement sign blocks, US Letter, dark-mode prints light. Hubs cross-link.
- vvsvei (hitthe.link/vvsvei): white clear-glass thumbs up/down, hover-only, learning signal.
- Gateway reboot-persistence (#5 done). CloudMounter invalid-filename fixed Mac+Nitro (1200+ files) + self-heal daemon.
- guardian + subagent prompt pages (hitthe.link/guardian, /subagent). Found mmm.xlrd.org = live "MIRROR omninbox" (prior reply-routing capability).

## HERMES CONDUCTOR — PARALLEL LANES (built + proven 2026-06-25)
qi's ask: "Claude sessions orchestrated in parallel, real-time omni-narrated, responded to in parallel at the same time." Built `E:\xen\xen-conductor.js` (HTTP :8030). Hermes = CONDUCTOR+NARRATOR+FAST-RESPONDER; lanes do the reasoning.
- KEYSTONE: `claude -p` IS logged in on Nitro now (stale canon corrected). Lane = persistent headless `claude -p --input-format stream-json --output-format stream-json --dangerously-skip-permissions --session-id <uuid> [--model]`, NDJSON over stdin. **NEVER `--bare`** (= "Not logged in" on Nitro). Spawn cwd+CLAUDE_CONFIG_DIR=E:\.claude.
- Routing: `/inject {text,scope?,kind?}` — conversation→always-on `fast` lane (haiku, instant, parallel); work→scope→lane (spawn on demand, cap 6), fire-and-narrate. Narration = flood-safe summaries → narrate-queue.txt → VVS.
- PROVEN: laneA+laneB ran concurrently while a conversation answered on fast lane. Reboot-safe via Startup\start-conductor.bat.
- CUTOVER LIVE (2026-06-25): inject-queue-daemon.ps1 now asks conductor `POST /route` for every queued line. Routing: trivial acks/greetings -> fast haiku lane (instant, via VVS); `[scope:xxx] ...` -> parallel work lane; EVERYTHING ELSE -> the live warm home pane (full context, instant). FAIL-SAFE: conductor down/slow (3s timeout) -> daemon injects to pane = exactly pre-cutover behavior, qi never mutes. Daemon backup: inject-queue-daemon.ps1.bak-precutover. Conductor restart = canonical ensure-inject-daemon.ps1 (hidden, keeps foreground perm).
- AUTONOMY (2026-06-25 ~5AM): conductor-watchdog.ps1 (hidden, 30s) keeps conductor continuously running/self-healing; Startup\start-conductor.bat = reboot-safe. AUDIT TRAIL: append-only conductor-audit.jsonl (every route decision + lane turn). LIVE FEED: :8030/feed (dark, mobile-first, SSE /events + /audit) = auditable lane progress visible in real time ("the real value is auditing"). VISION captured: canon\VISION-agent-marketplace-2026-06-25.md (skill-based agent marketplace, subscriptions, per-client encrypted provider, team-visible channel) — RAW 5AM dictation, refine before building.
- KNOWN: fast lane is context-free (only acks routed to it, by design); home=warm pane has context. Two test lines ("are you online…") got injected into qi's pane during wiring before the non-blocking /route fix.
- OPEN: pre-warm work-lane pool to hide ~9s hook; bind scope registry to real scope_ids; stream live (not just done) narration to Hermes; conductor reboot-safe via Startup\start-conductor.bat (verify order: daemon fail-safe covers conductor-not-yet-up).

## IN-FLIGHT
- DCZ Stal'lyon transparent cutout (dcz/assets/stallyon-cutout.png) being placed: full-height artist figure + hero element (agent running). Ship+open when done.
- Canon spine (this) + auth-bridge: being built now per "all of the above".
- DCZ self-serve EDIT PORTAL — SHIPPED 6/26. Client edits text at https://hitthe.link/dcz/edit/ (PIN=stallyon26) -> POST to Nitro Funnel https://nitro.salmon-alkaline.ts.net/dcz-edit (server E:\.xen\dcz-edit-server\server.js, port 8788, logon-start vbs in Startup) -> writes dcz/content.json -> commits+pushes -> GH Pages rebuild -> live overlay script renders content.json over defaults. E2E verified (rev3, sha 3a172360). Also added LaTina's 3 YouTube tracks (Love In The Day Time / Rhythm Ryderz Jingle / A Blessing) -> 12 tracks live. To change PIN: set DCZ_EDIT_PIN env or edit server.js + restart node. CF Worker path was blocked (token lacks account scope); Funnel-on-Nitro chosen.

## PENDING / BLOCKED ON QI
- M_B merch capability + quote — needs candle names.
- Mac BrowserOS :9001 as 2nd browseros MCP — needs brain restart (kills session).
- IG feed embed on DCZ — @diamondcutzstudio is login-gated; needs widget connected via qi's IG (go?).
- SuperAssistant in Perplexity — pointed at gateway /v1 (OpenAI) as MCP SSE = wrong protocol; needs real MCP endpoint OR use as model. Can wire.
- Invoice (dcz/invoice DCZ-001, $111 balance) — SHIPPED 6/25: qi said "send the invoice"; real invoice link https://hitthe.link/dcz/invoice/ delivered to LaTina (GV 467) + Andre (GV 396) with live Cash App (tsfwgtwl) + PayPal ($111 balance) pay buttons. Now awaiting client payment. NOTE: an OLDER, superseded invoice ($333/$166.50, Beside AI included) is STILL LIVE at https://hitthe.link/diamond-cut/invoice/ and was the link pasted to client back on 6/16-17 — risk of paying wrong amount; should be redirected/retired to dcz/invoice DCZ-001.
- NotebookLM full (302/300) — other half of #15, still open.

## NEXT (unified-brain build order, per PRD)
1. (done) auth-bridge token extractor → shared file.
2. (DONE 6/25) canon spine + LAST_KNOWN_STATE + protocol + wired into E:\.claude\CLAUDE.md (cold-start read + write-back ACTIVE).
3. scope registry (300+ clients: scope_id/enterprise/client/tools/auths/ports/status/urgency/execution_policy).
4. verbatim chunked session capture → Elasticsearch; Hermes curator; alias routing.

## WHAT QI WAS DOING
Reviewing DCZ live + sending the Stal'lyon cutout; injected the full unified-memory PRD; said "all of the above" → build auth-bridge + canon spine (+ finish DCZ).
